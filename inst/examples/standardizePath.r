\dontrun{

standardizePath()

path <- c(".", "..", "~", R.home(), tempdir(), NA)
standardizePath(path = path)
standardizePath(".")
standardizePath(".", sep = "\\")

standardizePath(path = character()

}
